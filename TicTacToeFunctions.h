#include <iostream>
#include <string>

void turnos(std::string& board);
bool checkWin(std::string& board);
int playToIndex(int play); //Converte o número jogado pelo jogador no index da casa correspondente